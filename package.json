{
  "scripts": {
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "gatsby-plugin-manifest": "latest"
  },
  "devDependencies": {
    "gh-pages": "latest"
  }
}
